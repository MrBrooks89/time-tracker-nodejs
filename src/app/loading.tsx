export default function Loading() {
  return (
    <main className="planner-bg flex min-h-screen flex-1 items-center justify-center overflow-hidden">
      <div className="blueprint-surface flex items-center justify-center px-8 py-6">
        <p className="micro-label animate-pulse">LOADING WORKSPACE</p>
      </div>
    </main>
  );
}
